cmake_minimum_required(VERSION 3.16)
project(prog3_fundamentos_task_2_v2026_2 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

file(GLOB_RECURSE student_sources CONFIGURE_DEPENDS src/*.cpp src/*.cc src/*.cxx)
add_executable(${PROJECT_NAME} main.cpp ${student_sources})
target_include_directories(${PROJECT_NAME} PRIVATE include src)

function(resolve_case_insensitive expected output)
    file(GLOB_RECURSE candidates CONFIGURE_DEPENDS RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}"
        "include/*" "src/*")
    string(TOLOWER "${expected}" expected_lower)
    set(matches "")
    foreach(candidate IN LISTS candidates)
        string(TOLOWER "${candidate}" candidate_lower)
        if(candidate_lower STREQUAL expected_lower)
            list(APPEND matches "${candidate}")
        endif()
    endforeach()
    list(LENGTH matches match_count)
    if(match_count GREATER 1)
        message(FATAL_ERROR "Ambiguous submission file for ${expected}: ${matches}")
    endif()
    if(match_count EQUAL 1)
        list(GET matches 0 match)
        set(${output} "${match}" PARENT_SCOPE)
    else()
        set(${output} "" PARENT_SCOPE)
    endif()
endfunction()

function(add_public_question question header source)
    resolve_case_insensitive("include/${header}" resolved_header)
    resolve_case_insensitive("src/${source}" resolved_source)
    file(GLOB_RECURSE test_sources CONFIGURE_DEPENDS
        "public_tests/question_${question}/test_*/*.cpp")
    if(NOT test_sources)
        message(FATAL_ERROR "No se encontraron pruebas publicas para la pregunta ${question}")
    endif()
    set(question_sources public_tests/test-main.cpp ${test_sources})
    if(resolved_source)
        list(APPEND question_sources ${resolved_source})
    endif()
    add_executable(public_test_question_${question} EXCLUDE_FROM_ALL ${question_sources})
    target_include_directories(public_test_question_${question} PRIVATE
        include src tools/catch public_tests)
    target_compile_definitions(public_test_question_${question} PRIVATE
        QUESTION_HEADER_PATH="${resolved_header}")
    add_test(NAME public_test_question_${question} COMMAND public_test_question_${question})
endfunction()

enable_testing()
add_public_question(1 "average.h" "average.cpp")
add_public_question(2 "book_search.h" "book_search.cpp")
add_public_question(3 "distance.h" "distance.cpp")
add_public_question(4 "fraction_vector2.h" "fraction_vector2.cpp")
add_public_question(5 "polynomial_matrix.h" "polynomial_matrix.cpp")
add_public_question(6 "permission_gray_image.h" "permission_gray_image.cpp")
add_public_question(7 "grade_filter.h" "grade_filter.cpp")
add_public_question(8 "message_format.h" "message_format.cpp")
add_public_question(9 "price_rule.h" "price_rule.cpp")
add_public_question(10 "report_exporter.h" "report_exporter.cpp")
add_custom_target(public_tests_all DEPENDS public_test_question_1 public_test_question_2 public_test_question_3 public_test_question_4 public_test_question_5 public_test_question_6 public_test_question_7 public_test_question_8 public_test_question_9 public_test_question_10)
