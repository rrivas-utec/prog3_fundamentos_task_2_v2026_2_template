# Task # Ejercicios - Sobrecarga y polimorfismo dinámico
**course:** Programación III  
**unit:** Fundamentos  
**cmake project:** prog3_fundamentos_task_2_v2026_2
## Indicaciones Específicas
El tiempo límite para la evaluación es 390 minutos.

Cada pregunta deberá ser respondida en un archivo cabecera dentro de `include/` y, cuando corresponda, en un archivo fuente dentro de `src/`:

 - `include/average.h` `src/average.cpp`
 - `include/book_search.h` `src/book_search.cpp`
 - `include/distance.h` `src/distance.cpp`
 - `include/fraction_vector2.h` `src/fraction_vector2.cpp`
 - `include/polynomial_matrix.h` `src/polynomial_matrix.cpp`
 - `include/permission_gray_image.h` `src/permission_gray_image.cpp`
 - `include/grade_filter.h` `src/grade_filter.cpp`
 - `include/message_format.h` `src/message_format.cpp`
 - `include/price_rule.h` `src/price_rule.cpp`
 - `include/report_exporter.h` `src/report_exporter.cpp`  

Deberás subir estos archivos directamente a www.gradescope.com o se puede crear un .zip que contenga todos ellos y subirlo.

## Enunciado

Consulta `enunciado.pdf` en la raíz del proyecto. Es la fuente de verdad para
los requisitos, firmas, ejemplos y criterios de cada pregunta.

## Public tests in your IDE

Open the project with CMake in CLion or VS Code. Run only the target for the
question you are implementing, for example `public_test_question_1`. The
`public_tests_all` target is intended for the final review of all questions.
